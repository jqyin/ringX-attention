from setuptools import setup, find_packages

setup(
    name='ringX_attn',
    version='0.1',
    packages=find_packages(),
    install_requires=[
       #'torch>=2.0.0'
       #"flash_attn"
    ],
)
